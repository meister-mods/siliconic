package io.github.meistermods.siliconic.cleanroom;

import net.minecraft.world.level.block.Block;

/** A coated cleanroom boundary block that links cables mounted directly to its faces. */
public class CableCoatedBlock extends Block {
  public CableCoatedBlock(Properties properties) {
    super(properties);
  }
}
